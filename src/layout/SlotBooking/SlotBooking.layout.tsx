import { Outlet } from 'react-router-dom';

import { Stack } from '@mui/material';

export const SlotBookingLayout = () => (
    //TODO: Add Booking Layout Here
    <Stack>
        <Outlet />
    </Stack>
);
