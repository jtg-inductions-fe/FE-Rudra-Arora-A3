import { Stack, Typography } from '@mui/material';

const NoData = () => (
    <Stack alignItems="center">
        <Typography variant="h2">No Data Availaible</Typography>
    </Stack>
);

export default NoData;
